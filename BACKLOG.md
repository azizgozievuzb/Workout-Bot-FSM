# 📋 BACKLOG — будущие фичи (не привязаны к текущей сессии)

> Сюда складываем идеи, которые не делаем прямо сейчас, но НЕЛЬЗЯ забыть.

---

## 🆕 После 7.5 «Всё через Stars» (Session 45, 2026-07-18)

1. **Онбординг вариант B — витрина тарифов ВНУТРИ приложения до оплаты (конверсия).**
   Сейчас новый юзер видит пейволл сразу (вариант A). Вариант B: сначала показать ценность/фичи/демо,
   пейволл — в момент действия. Делать перед публичным запуском, A/B на конверсию. (Решение юзера, Session 44 п.14.)
2. **Авто-скидка первого платежа на сервере без ввода кода** (Session 44 п.6a) — если «первый месяц дёшево» нужно массово каждому новому юзеру, а не через раздачу купонов. Сейчас закрыто intro-ценой per tier (настраивается в админке); эта опция — если захотят ещё дешевле/гибче.
3. **Настройка Ответственного после оплаты (имя/пол)** — сейчас fulfill ставит onboarding_done=true, отдельного экрана имя/пол для Ответственного нет (берём first_name из Telegram). Добавить мини-экран при желании (Session 44 п.14 упоминает «имя/пол»).
4. **Job A/B — живой smoke aiogram `send_invoice` с `pay=True` кнопкой** в чате (счёт-напоминание). Проверить, что нативная кнопка оплаты появляется и ведёт в pre_checkout → successful_payment (tier fulfill).

---

## 🆕 Session 47 (2026-07-21) — после дизайн-сессии (гейт/подписка/цены)

1. **Гибрид старта первого платежа**: отсчёт 30 дней запускается при первом принятом инвайте, но не позже 7 дней после оплаты (авто-старт job-ом). Контекст: Session 47 решил «отсчёт с оплаты», идея «сгорания неиспользованной подписки» отвергнута. Делать ТОЛЬКО при реальных жалобах «оплатил, игрок подключился позже — потерял дни». Касается только первого платежа, продления не трогать.

## 🆕 Session 48 (2026-07-22) — smoke 7.5.1

1. **Пер-тирные «плюшки» (перки) для игроков.** Решение юзера: в будущем тарифы будут отличаться
   не только лимитом слотов — у игроков появятся перки, зависящие от тира их Ответственного.
   При реализации: перки выводить ТОЛЬКО из живой подписки Ответственного (authStore/deps.py),
   НЕ заводить per-player снапшоты тира (бейджи с карточек убрали в Session 48 именно из-за вранья снапшота).

## 🆕 Session 54 (2026-07-26) — смоук 8c, находки юзера

1. ✅ **[встроено в 8d]** Полная страница игрока в R-панели (находка №9) — вошла фундаментом в постановку 8d (PROMPT_8D, S55).
2. **[8e/предзапуск] «Чистый чат»** (юзер, находка №10): служебные сообщения бота копятся в чате навсегда. Политика: техсообщения/напоминания — авто-удалять (`deleteMessage`, окно 48ч) или редактировать в короткую строку; квитанции подписок — ОСТАВЛЯТЬ (чеки); системные Stars-трансферы неудаляемы (ограничение Telegram). Реализация не сейчас.
3. ✅ **[решено S55 → 8d]** Бусты X2 — выпил целиком в 8d (решение юзера 2026-07-29; переупаковка через полку возможна позже — кандидат в S50 №4 остаётся).
4. **[8e] Тексты repeat-карточки «Готово»** (юзер, находка №16 смоука 8c): повторная ПОЛНАЯ сессия, закрывшая день, показывает «День закрывает только полная сессия» — читается как упрёк человеку, который её только что сделал. Различать: повторная частичная → текущий текст; повторная полная, закрывшая день → «✅ День закрыт — стрик спасён».
5. **[8e] Pressed-анимации и async-индикаторы глобально** (юзер, находка №15 смоука 8c): у кнопок нет pressed-состояний, у async-операций — индикаторов (например, «Загружаем фото…» после выбора селфи). Пройтись по всему UI: pressed-анимация + спиннер/текст на время запроса. (Double-submit guard кнопок покупок — раньше, в патче 8c.1.)

---

## 🆕 Session 58 (2026-08-04) — смоук 8d.1, находки юзера

1. **[эконом-сессия] Единый паттерн подтверждения необратимых трат капель.** Сейчас «Купить» на полке наставника списывает капли одним тапом, а рядом стоят «▶︎ Смотреть» и «Неинтересно» — промах пальцем стоит игроку реальных капель. Касается НЕ только полки: реролл фото, смена графика, восстановление стрика — те же необратимые траты, паттерн нужен один на все. **Решение юзера по форме:** обычное окно подтверждения с «Отменить» + «Купить»; отказ от подтверждений — «не спрашивать больше НА ЭТОМ УСТРОЙСТВЕ» (localStorage, не БД — новое устройство спросит заново, и это нормально), причём поставить отказ должно быть НЕЛЕГКО: не галочка у кнопки (её задевает тот же промахнувшийся палец), а поле ввода с инструкцией «напишите сюда `не спрашивать`». **Открытый вопрос (юзер не определился):** окно-подтверждение vs всплывашка с полем отказа — решать вместе с ценами, потому что «дорого/дёшево» определяет, насколько подтверждение вообще уместно. **Замечание CC:** нужен путь ОБРАТНО (вернуть подтверждения после отказа) — а это уже экран настроек; если экран настроек всё равно появляется, отказ логичнее держать там, и поле ввода становится лишним. Решать одним куском.
2. **[8e, из S54 №5] Мелкие кнопки без `:disabled` — закрыто частично.** ✅ В 8d.1 добавлено правило `.cube-btn-sm:disabled` (блёклость + курсор). Остаётся системная проверка: пройтись по всем классам кнопок проекта и убедиться, что у каждого есть и pressed-, и disabled-состояние — сейчас они добавлялись точечно, по мере находок смоуков.
3. **[8e] Отчёты игрока — отдельная страница-архив вместо секции на полке** (юзер, смоук 8d.1 п.11д). Сейчас (вариант A, сделан) на полке видны 3 последних отчёта + «Показать все (N) →»; бэк отдаёт максимум 10, ретеншн уносит строки через 30 дней после исполнения. Но по смыслу отчётам на полке не место: полка — МАГАЗИН наставника (лоты, слоты, долги), а отчёты — ретроспектива, они соревнуются за внимание с тем, ради чего наставник сюда зашёл. **Вариант B:** дом отчётов — рядом с репутацией: «🏅 Исполнено обещаний: N» → тап → отдельная страница-архив в стеке `MentorPlayerScreens` со всеми отчётами. Делать вместе с дизайном страницы репутации, тогда же снять лимит 10 и добавить подгрузку.
4. **[8e] Формат записи видео: webm на десктопе.** Рекордер берёт первый поддерживаемый кодек из списка `vp9 → vp8 → webm → mp4`; Chrome на macOS даёт `.webm`, который не открывается системным плеером (только браузером). У Cell на iPhone Safari webm не умеет — там уже `.mp4`. Починка — переставить mp4 в начало списка (`PromiseRecorder.tsx`), но это **смена кодека после того, как вес пресета C замерен именно на VP8/webm** (0.87 МБ за 15 с): H.264 при тех же 400 кбит/с даст другой вес и другое качество, п.5 смоука придётся перепройти. Делать одним куском с перезамером.

---

## 🆕 Session 57 (2026-07-30) — смоук 8d, дизайн-находки

1. **[ВАЖНО, экономика] Каталог Stars-предметов дублирует витрину игрока** (находка №24 смоука 8d). Наставник платит 50⭐ реальных денег за заморозку, а игрок выкупает её за те же 50💧, что и в своём магазине — ценности не создаётся, звёзды сожжены. Нижний лимит цены лота (50💧) не даёт наставнику даже субсидировать. Плюс риск застрявшего лота: при `paid_freezes=3` покупка вернёт `FREEZE_CAP`, звёзды уже списаны, выкупить нельзя. Развилки: (1) каталог только из эксклюзива — light-трайал на неделю, доп. слот полки, титул (см. S50 №4), плюс «заморозка сверх капа» и «сброс кулдауна смены графика»; (2) дать заморозке с полки структурное преимущество (не считать в кап 3 и/или разрешить цену ниже магазинной); (3) считать v1 заглушкой. **Рекомендация CC: 1+2.** Сюда же — игроку нигде не объяснено, почему один и тот же предмет есть в двух каналах.
2. **[частично закрыто в 8d.1] AI теряет портретное сходство** (находка №27). ✅ Сделано в 8d.1: два режима в одной генерации (`weak` — максимум сходства, `deep` — сильная стилизация), подписи режимов на экране выбора, оригинал селфи рядом с вариантами, опция «оставить как есть». **Остаётся:** глубокая калибровка сходства (отдельный трек вместе с Gemini calibration v2) и живой тюнинг формулировок промптов — скрипт готов: `python3 -m backend.scripts.tune_card_prompts` (платные вызовы, нужен GEMINI-ключ в `backend/.env` и селфи в `samples_8d1/`).
3. **«Постучаться в пустой слот полки»** (идея юзера, смоук 8d). Игрок тапает свободный слот → наставнику приходит нудж «игрок ждёт новое обещание». **Оговорка CC (обязательна к учёту):** механика разворачивает роли — игрок становится просящим, наставник перестаёт быть ведущим; при частом использовании скатывается в выпрашивание и давит не тем способом, каким давят невыполненные обещания. Если делать: лимит 1 стук в 3–7 дней, нейтральные формулировки, возможно разблокировка только после закрытой недели.
4. ✅ **[внедрено в 8d.1] Пресет записи видео — вариант C.** Оба рекордера (обещание наставника и видеоотчёт игрока): **15 сек**, **480×640**, видео **400 кбит/с** через `videoBitsPerSecond`, аудио **64 кбит/с** моно, **30 fps**; разрешение запрашивать у `getUserMedia`, битрейт ограничивать на самой записи (без перекодирования на клиенте). Ожидаемый вес 15 с ≈ 0.8 МБ вместо ≈ 17.9 МБ. **Обоснование:** живой замер показал, что MediaRecorder пишет без ограничения битрейта (~1.2 МБ/сек), и при лимите 30 сек ролик ~36 МБ упёрся бы в потолок `MAX_PROMISE_BYTES` = 30 МБ — то есть длинное обещание сейчас не загрузилось бы вовсе. Подробности и сэмплы — SESSION_STATUS S57.
5. ✅ **[закрыто патчем 8d.1] Пакет UX-долгов смоука 8d** — №17, №18, №19, №20, №23, №25, №26, №28. Реализовано, ждёт живого смоука (чек-лист — PROMPT_8D1.md).

## 🆕 2026-08-03 — родилось при реализации 8d.1 (CC)

1. **Ретеншн видео считается от `fulfilled_at`, а отчёт теперь можно приложить позже (Д6).** Игрок, приложивший отчёт на 29-й день после галочки, увидит его удалённым на 30-й — окно жизни файла привязано к моменту исполнения, а не к моменту загрузки. Сейчас это осознанно оставлено как есть (ретеншн — продуктовая политика §8.8a п.8, не UI-патч). Если станет заметно: считать ретеншн от `GREATEST(fulfilled_at, report_uploaded_at)` — потребует колонку под дату загрузки отчёта.
2. **Галерея отчётов у наставника ограничена 10 последними** (`REPORTS_LIMIT` в `shelf.py`) без пагинации. При активной паре старые отчёты станут недоступны раньше, чем их удалит ретеншн. Пагинация/«показать ещё» — когда появится живая пара с длинной историей.

## 🆕 2026-08-03 — дизайн-интервью 8d.1 (Cowork)

1. **[предзапуск/исследование] E2E-приватность видео-обещаний: «даже админ не может открыть»** (идея юзера, 2026-08-03). Контекст: обещания могут быть очень личными; нужно шифрование, при котором видео пары открывают только наставник и игрок — включая невозможность просмотра админом/владельцем БД. Сейчас приватный бакет + подписанные ссылки защищают от посторонних, но НЕ от админа с service-ключом. **Оговорка Cowork (честная оценка):** настоящий E2E в Telegram WebView — большой отдельный проект: ключи негде надёжно хранить (storage webview чистится), мультидевайс требует переноса ключей между устройствами (живой кейс пары iPhone+десктоп уже есть, S54), потеря ключа = все видео пары потеряны навсегда, «восстановить» невозможно по определению E2E. Практичный первый шаг вместо этого: журнал доступов к видео (кто и когда открывал, виден паре) + заявленная политика «админ не смотрит»; полный E2E — только после стабилизации продукта, отдельным треком. При проработке проверить: видеоотчёты обещаний в Gemini НЕ уходят (иначе E2E сломает пайплайн оценки).
2. **[после масштабирования] Standalone-приложения iOS/Android** (идея юзера, 2026-08-03). При удачном росте вынести продукт за пределы Telegram: отдельные приложения в App Store / Google Play, та же БД/аккаунты. **Оценка Cowork: НЕ с нуля** — React-фронт и весь бэкенд/Supabase переиспользуются на ~70–80% (путь: обёртка Capacitor поверх текущего веб-кода). Заменяются только «края», привязанные к Telegram: авторизация initData → своя auth (Supabase Auth: телефон/email); **Telegram Stars → нативные IAP Apple/Google — самая болезненная замена** (комиссия 15–30%, другие правила подписок, экономика сейчас завязана на Stars); бот-уведомления → push (APNs/FCM); @telegram-apps/sdk (haptics/back/theme) → нативные аналоги. Камера/getUserMedia в нативной обёртке работает. Риск: Apple guideline 4.2 против «тонких обёрток» — полноценный продукт с камерой и платежами проходит.

---

## 🆕 Session 50 (2026-07-23) — из дизайн-ревизии Фазы 8

1. **ИИ-рандом-факты по полу** (юзер): приложение/бот кидает игроку рандомные актуальные факты — «девчачьи» для Ж, «мужские» для М. Генерация/курирование пула + доставка (уведомления?). Делать ПОСЛЕ ядра Фазы 8 (гендерные тексты-словари войдут в Ф8, факты — нет).
2. **Snooze-лимиты уведомлений** (3/год бесплатно, сверх — за капли, «2 подряд → нужна помощь?») — вырезано из ядра Ф8 (тема 3 ревизии), вернуться при живой статистике поведения.
3. **Талон замены упражнения** (Cowork): разово заменить упражнение в сессии на аналог той же группы за капли. Отклонён юзером для Ф8 как сложный (пул альтернатив + логика сессии); идея признана сильной — на потом.
4. **Расширение каталога Stars-предметов для полки R** (юзер, тема 7 ревизии): v1 = заморозка + реролл фото, «очень маленький — придумать ещё». Кандидаты: буст X2 через полку (переупаковка существующего), «light-режим на неделю» (трайал перед покупкой unlock), доп. слот полки, титул/бейдж в подарок. Добавление предмета = строка в каталоге, без миграций — расширять по мере надобности.
5. **Перки тиров — кандидаты за пределами Ф8**: +1 бесплатная заморозка/мес игрокам elite-наставника (Ж4/М2), скидка на drop packs для prm/elt.
6. **Монитор обновлений платформ и зависимостей** (юзер, S54): чтобы приложение не отставало от обновлений iOS/macOS/Telegram/aiogram. Что сделать, когда дойдём: (а) включить **GitHub Dependabot** (или Renovate) на репо — авто-PR при новых версиях pip/npm-зависимостей (aiogram, react, supabase-js…); (б) подписаться на **@BotNews** в Telegram (официальный канал изменений Bot API) и releases aiogram на GitHub (Watch → Releases); (в) ежемесячный «дежурный чек» (можно scheduled-задачей в Cowork): свежие iOS/TG-версии → быстрый смоук критичных путей (камера, оплата, webview). Реализация — до публичного запуска.

**Устарело (7.5 выпилила промо-как-оплату):** все пункты ниже про R/P/renewal/bonus-pack промокоды, генерацию кодов, `promo_codes` — НЕ актуальны. Промокод теперь = купон на скидку (таблица `coupons`), вводится при оплате тарифа. Секцию «Тиры/подписки за Stars» ниже считать реализованной разовыми платежами (нативные подписки отвергнуты, Session 44 п.4).

---

## 🔍 Codex audit · Session 38 (2026-06-05)

Найдено внешним аудитом во время Session 38. Репо приватный → не блокеры для 7.3 smoke, но обязательно разгрести до публичного релиза / 7.4.

1. **Утечка Gemini API-ключа** — `research/compare_quality.py:6` содержит реальный ключ, плюс старые `PROMPT_AI_PHOTO.md` и предыдущие версии `SESSION_STATUS.md` в git history. Ротировать в AI Studio + удалить файл + `git filter-repo` для чистки истории. Когда: перед любым расшариванием репо или в начале 7.4. **(остаётся открытым — решение юзера не трогать в 7.4, репо приватный)**
2. ✅ **ЗАКРЫТО в 7.4 (Session 43).** Webhook больше не логирует секрет — `backend/main.py:133` печатает только факт mismatch.
3. ✅ **ЗАКРЫТО в 7.4 (Session 43).** `validate_init_data` отвергает initData старше `INITDATA_MAX_AGE_SEC=86400` (config) — `backend/core/security.py`.
4. **AdminCube `total_stars_earned`** — уже отдельной секцией ниже, оставить.
5. ✅ **ЗАКРЫТО в 7.4 (Session 43).** `POST /boosts/buy` удалён; активация буста только через `successful_payment` → `services/boost_service.activate_boost`. Оплата обязательна.
6. **`finish_session` не апдейтит `global_score`/`three_day_score`** — `backend/api/routers/admin.py:385` показывает workouts-done из устаревших полей. Либо чинить запись в finish_session, либо в admin читать реальный count из workout_sessions. Когда: Phase 8 (там админ-панель пересобирается).
7. **Видео-ретеншн 7 дней — только комментарий в `017_workout_sessions.sql:65`**. Реализовать APScheduler-таску. Когда: Phase 8 (GDPR-блок).
8. **FE FSM reducer принимает поздний `AI_VERDICT` в любом состоянии** — `frontend/src/fsm/workoutSessionMachine.ts:73`. БД корректна (upload использует captured `exercise_idx`), но локальный стейт может писать в чужой index если AI вернётся после auto-next. Когда: после первого реального бага из-за этого или вместе с переработкой `200_workoutSessionMachine`.
9. ✅ **ЗАКРЫТО в 7.4 (Session 43).** `shop_items.price_stars → price_drops` (миграция 029, idempotent) + весь стек (`shop.py`, `shop.ts`, `MarketCube.tsx`). `promo_codes.price_stars` не тронут. NB: `star_products.price_stars` — намеренно реальная цена в Stars (новая таблица 028).
10. **`014_tiers_bans_maintenance.sql` / `023_rename_star_balance_to_xp.sql` неидемпотентны** — нет `IF NOT EXISTS`. При повторном применении упадут. Не критично сейчас (применены), но style-guide на новые миграции — всегда idempotent.

---

## ⭐ Тиры/подписки за Telegram Stars (поверх инфры 7.4)

**Контекст:** в 7.4 сделана разовая покупка бустов через Stars (invoice → pre_checkout → successful_payment → fulfill). Для тарифов Responsible (standard/premium/elite) логичнее нативные **Stars-подписки**, а не разовые платежи.

**Что делать:** использовать `subscription_period` в `createInvoiceLink` (Bot API 8.0+, только 30 дней/2500⭐ лимиты Telegram) + обрабатывать `BotSubscriptionUpdated`-апдейты (появились в Bot API 10.2) для авто-продления/отмены. Хранить подписки в отдельной таблице, связать с `responsible_access_tier`.

**Когда:** когда тиры начнут продаваться напрямую за Stars (не промокодами). Инфраструктура `payments`/`star_products` переиспользуется.

---

## ✅ ЗАКРЫТО (S57) · Tech-debt: AdminCube `total_stars_earned` — ✅ ЗАКРЫТ (Session 56, 2026-07-29)

Починен попутно при включении настоящего tsc-гейта в смоук-сессии 8d (`total_stars_earned` → `total_xp_earned`; ⭐undefined в карточке Responsible ушёл). Там же удалён мёртвый `ensureMounted()` в haptic.ts.

---

## 🎬 Демо-видео упражнений — naming convention для метаданных

**Контекст:** сейчас демо-видео в `frontend/public/demos/<key>.mp4` имеют только ключ упражнения в имени. Метаданные (position, muscles, hint) хранятся отдельно в `backend/core/workout_config.py`. Когда юзер будет снимать собственные демо — нужно зашифровать ВСЁ в имя файла, чтобы:
- проще было распознавать видео без открытия конфига
- метаданные не «терялись» при перезаливке/перенарезке
- на frontend можно было бы показывать положение камеры («Встаньте боком», «Повернитесь спиной») АВТОМАТИЧЕСКИ из имени файла

**Предлагаемый формат имени файла:**

```
<exercise-key>__<difficulty>__<goal>__<position>__<camera-angle>.mp4
```

Где:
- `exercise-key` — slug упражнения (`squats`, `pushups`, `plank`)
- `difficulty` — `easy` | `medium` | `hard`
- `goal` — `endurance` | `strength` | `cutting` | `bulking` (выносливость / сила / похудение / набор)
- `position` — `standing` | `sitting` | `lying-back` | `lying-front` | `kneeling` | `plank-pos`
- `camera-angle` — `front` | `side-left` | `side-right` | `back` | `top-down`

**Примеры:**
```
squats__medium__endurance__standing__front.mp4
plank__hard__strength__plank-pos__side-left.mp4
crunches__easy__cutting__lying-back__front.mp4
```

**Что делать в коде когда дойдём:**
1. Backend: парсер имени файла → извлекает метаданные при загрузке. Поля `Exercise` обогащаются: `difficulty`, `goal`, `position`, `camera_angle`.
2. Frontend: на rest-экране показывать «Встаньте боком к камере / лицом к камере / спиной к камере» по `camera_angle`.
3. Frontend: фильтрация в Shop по `goal` и `difficulty` (для tier-based доступа).
4. Backend: при отсутствии метаданных в имени — фолбэк на дефолты + warning в логи.

**Когда делать:** когда юзер будет снимать собственные демо (после успешного прохождения 7.3-7.4 и переноса на полноценный продакшен).

**Связанные задачи:** content debt по замене 16 текущих демо (нарезанных Antigravity-агентом, не точно соответствующих названиям).

**Идея юзера (2026-07-16) — reference-based оценка:** когда появятся правильные собственные демо, Gemini должен оценивать выполнение игрока СРАВНЕНИЕМ с эталонным демо-видео (передавать в запрос оба видео: клип игрока + демо), а не по текстовому описанию/hint. Это же решает проблему завышенных оценок (см. §8.15 Gemini calibration v2) — модель видит эталон темпа/амплитуды. Порядок: сначала снять корректные демо → потом reference-based промпт. Фаза 8, вместе с calibration v2.

---

## 🔄 Recovery сессии при случайном закрытии (промпт B)

**Контекст:** iOS Telegram горизонтальный pager-свайп невозможно полностью отключить. Если юзер случайно свайпнул — тренировка убивается. Сейчас спасает только дисклеймер + auto-сохранение каждого упражнения в `workout_clips`. Но активная сессия не возобновляется.

**Что делать:**
1. Backend: добавить колонки `last_phase`, `last_exercise_idx`, `last_phase_started_at` в `workout_sessions`. Обновлять на каждом TICK от frontend.
2. Backend endpoint: `GET /workout/active-session` — возвращает активную незавершённую сессию юзера (если есть).
3. Frontend: на mount WorkoutScreen — проверка active-session. Если есть и менее 30 мин с `last_phase_started_at` → модал «Продолжить тренировку с упражнения N?» / «Начать заново».
4. FSM: добавить событие `RESUME_FROM` для входа в произвольное состояние с произвольным `currentExercise`.

**Когда делать:** после 7.4 (Stars payments) — это улучшение UX, не блокер MVP.

---

## 🟦 Фаза 8 — Workout Types & Drops Economy (DESIGN DOC v2)

> **Переработан в Session 50 (2026-07-23)** по итогам дизайн-ревизии (9 тем, все решения юзера — в SESSION_STATUS S50). Версия S36 устарела и заменена этой. Реализация: подфазы **8a–8e** (см. 8.16), каждая — отдельный промпт для CC.

### 8.1 Типы тренировок
- **Main** (текущий формат): 16 упражнений × ~26 мин, макс **50 💧**, формула `50 × quality × (done/16)^0.65 × streak_mult`.
- **Light**: 4 упражнения × ~20 мин (3 мин работа + 1 мин отдых ×4 + разминка/заминка), макс **30 💧**, формула `30 × quality × (done/4)^0.65 × streak_mult`. Контент: ходьба на месте, подъём коленей, пятки к ягодицам, маршировка (нужны демо-видео — контент на юзере). Позиционировать как «зарядка/ритуал», НЕ «тренировка». По умолчанию закрыт — покупается за капли (8.4).
- **Антифарм (решение юзера, смоук S52, реализация в 8c):** начисления идут максимум за 1 main + 1 light сессию в день — И капли, И **XP** (в 8b XP ещё фармился — закрыть). Повторные сессии дня: без цифр вообще; карточка «Готово» вместо начислений показывает поддерживающую фразу («Ты молодец!», «Красавчик, телу спасибо!» — пул фраз, в 8c нейтральные, гендеризация в 8e). Тренироваться сверх плана не запрещаем.
- **Ранний выход и закрытие дня (решение юзера + компромисс Cowork, S52→8c):** красная кнопка = «Закончить досрочно» → **частичный зачёт** капель/XP по формуле `(done/N)^0.65` (если это первая сессия дня с начислениями). НО **стрик-день закрывает ТОЛЬКО полная сессия** (все N упражнений). Модалка раннего выхода: «Засчитано X из N, +Y 💧. День НЕ закрыт — пройди полную зарядку/тренировку до конца дня, чтобы спасти стрик». Повторная ПОЛНАЯ сессия до полуночи закрывает день (начисления 0 — антифарм). Не доделал до полуночи → штатный closure (заморозка или слом). Идея «одна попытка в день, повторная не закрывает» — отвергнута (наказание за жизнь, а не за лень; мотивация против себя).

### 8.2 Расписание
- 3 main-дня в неделю, выбор на онбординге (новый шаг опроса); можно 3 подряд, но с warning. Существующим игрокам — экран довыбора при первом входе после релиза 8a.
- После unlock light — 7-дневный график (3 main + 4 light).
- Недельный reset: вс 23:59 → пн 00:00 по локальной TZ юзера.
- **Смена графика:** применяется со следующего понедельника + кулдаун 30 дней + платно за капли; первые 14 дней после онбординга — бесплатно и без кулдауна. Кнопка называется «Изменить дни main-тренировок» и НЕ прячется в light-режиме (график = какие дни тяжёлые). Продаётся контекстно в настройках расписания, НЕ карточкой витрины.

### 8.3 Стрик и заморозки
- До unlock light: стрик ломает только пропуск main-дня. После: пропуск любого дня. Новый режим — со следующего понедельника после unlock/lock.
- **Заморозки (rest days):** бесплатные — **Ж 3/мес, М 1/мес** (фикс, без привязки к режиму; логика юзера: заморозки под критические дни, «мужчина консервативен — справится, купит»). Не накапливаются месяц-к-месяцу. Докупка за капли всем (кап запаса докупленных: 3). **Авто-срабатывание**: при пропуске дня сгорает заморозка (сначала бесплатная, потом купленная) вместо стрика + уведомление. R может дарить заморозки (существующее «Подарить заморозку»).
- **Restore стрика** за капли — контекстно на экране сломанного стрика: `20 💧 × дней` (мин 60, кап 400). Формула `10×days²` из S36 отменена.
- **Смена пола после онбординга — только через админку** (анти-фарм заморозок).

### 8.4 Light unlock / lock
- **Unlock** — покупка за капли, навсегда. **Lock** (отмена, «я пока не готов») — тоже ПЛАТНАЯ карточка, дороже unlock ×1.5–2; после lock стрик снова main-only со следующего пн. Повторный unlock — обычная цена. Множитель ×3 и 6-мес сброс из S36 — отменены. Перед unlock и lock — дисклеймер-тест (8.10).

### 8.5 Уведомления (ядро; лимиты — BACKLOG)
- Макс 2/день: утреннее в день тренировки (время выбирает юзер в настройках) + вечернее, если тренировка не сделана. Inline-кнопки в Mini App. Тихое окно 22:00–09:00 локального времени.
- «⏰ Напомнить вечером» — бесплатно, без счётчиков. Snooze-лимиты (3/год, оплата каплями, «2 подряд → нужна помощь?») — в BACKLOG S50 до живой статистики.
- Тексты в 8a нейтральные; гендерные словари — 8e.

### 8.6 Балансы (упрощены против S36)
- У игрока — **один** `drops_balance` (заработанные + подаренные вместе). `gift_balance` — **только у Responsible** (закупленный пул для дарения). Двухколоночная модель у игрока отменена.
- В режиме Responsible свой личный баланс не виден. Переключение роли при активной тренировке → тренировка проваливается.

### 8.7 Responsible
- Покупает **drop packs за Stars** (единственный способ появления капель извне; сам игрок капли за деньги купить НЕ может — анти pay-to-win). Пакеты (плейсхолдеры): 100 💧 = 100 ⭐, 300 💧 = 250 ⭐, 1000 💧 = 700 ⭐; цены в админке. Product_type поверх платёжки 7.4.
- Дарит капли игроку (перевод из gift_balance + уведомление). Кап дарения не вводим (щедрый R — фича).
- Ведёт полку игрока (8.8), видит XP-прогресс, НЕ видит баланс капель игрока.
- В карточке игрока видит: пол, цель, подготовку (дисклоз в онбординг-опросе: «будут видны наставнику»).

### 8.8 Магазин игрока — 2 полки

**Полка приложения — ровно 4 карточки (решение юзера):**
| Карточка | Цена (плейсхолдер) | Заметки |
|---|---|---|
| Unlock light | 300 💧 | дисклеймер-тест |
| Lock light | 500 💧 | дисклеймер-тест |
| Заморозка | 50 💧 | кап запаса 3 |
| Фото-карточка | 200 💧 + реролл 60 💧 | см. 8.8b |

Контекстные стоки вне витрины: restore стрика (экран слома), смена графика (настройки).

**Полка наставника** (слоты по тиру: **std 2 / prm 4 / elt 6** на игрока — это второе отличие тарифов после лимита игроков):
1. **Видео-обещания** (8.8a) — главный контент полки.
2. **Stars-предметы**: R покупает предмет за Stars из каталога приложения → кладёт на полку с ценой в каплях (ставит сам, в админ-лимитах) → игрок покупает трудом. Каталог v1: заморозка, реролл фото. Расширение — BACKLOG S50.
- Цены товаров R — в админ-лимитах 50–2000 💧. «Мне это неинтересно» → товар скрыт, R уведомлён. Маркировка «🎁 от наставника».

### 8.8a Видео-обещания (решение юзера, S50)
Товар наставника = реальное офлайн-обещание, **записанное на видео** (≤30 сек, среднее качество):
1. Создание: R записывает видео (проговаривает обещание) + название + цена → слот полки.
2. Игрок смотрит предпросмотр ДО покупки, но видео «не его».
3. Покупка: списание капель → видео в «Мои покупки» игрока → у R «к исполнению».
4. Дискомфорт R до исполнения: постоянный бейдж «⏳ N обещаний», плашка в карточке игрока, напоминания ботом ~раз в 3 дня. Гаснет ТОЛЬКО по галочке.
5. **Галочку «Выполнено» ставит игрок**; опционально прикладывает видеоотчёт «как это было».
6. После галочки: у R счётчик репутации «исполнено обещаний на N 💧», у игрока — история трат.
7. Споры: авто-возврата нет; вечный pending давит на R; возврат капель — админ вручную.
8. **Хранение: только Supabase Storage** (подписанные ссылки, доступ только паре), ретеншн «галочка + 30 дней». Кнопка **«Сохранить в галерею»** (выгрузка на устройство). В чаты бот видео НЕ шлёт. Ёмкость: 1000 юзеров × 1/нед ≈ 6 ГБ/нед, steady-state с ретеншном ~30–40 ГБ.

### 8.8b Фото-карточка (решение юзера, S50)
- У наставника ВСЕ игроки по умолчанию — мультяшный образ по полу (приватность by default; поведение «styled-фото у R» из 7.5.1 меняется). Styled-фото остаётся бэкграундом у самого игрока.
- Покупка «Своё фото на карточке»: AI-обработка селфи с гендерным промптом (женственнее/брутальнее; тот же стек photo_styler/Gemini image) → **2 варианта на выбор**; обязательна опция **«поставить без обработки»** (личное фото не отправлять в AI); в флоу загрузки обязательна кнопка «Сделать фото» (камера), не только галерея.
- **Прогрессирующая цена AI (решение юзера, смоук S54; реализация — патч 8c.1):** raw-смена (без AI) — цена фикс всегда. **AI-смена №k: базовая × 2^(k−1)** (200→400→800…, кап в meta, предложение 3200; счётчик AI-смен на юзере). **Реролл №m внутри одной смены: 60 × 2^(m−1)** (60→120→240…), счётчик рероллов сбрасывается при новой смене. Множитель и кап — админ-редактируемые. Мотив: каждая генерация жжёт реальные $ — без прогрессии фото будут менять бесконечно по базовой цене. UI показывает актуальную ДЛЯ ЭТОГО юзера цену на карточке.
- ⚠️ Единственный товар с реальным COGS (~$0.03–0.13/генерация) — цену реролла не занижать.

### 8.9 Pair-only philosophy — без изменений (реализовано в 7.5)
Игрок не существует без R; grace 3 дня; расторжение только через саппорт. **Safety button — предзапускной чек-лист.**

### 8.10 Дисклеймер-тесты (сокращено)
Тест из 3 вопросов (хардкод) — ТОЛЬКО перед: Unlock light, Lock light, смена графика. «Тест при покупке >100 💧» отменён — обычная confirm-модалка.

### 8.11 Drops decay — ПЕРЕНЕСЁН в предзапускной чек-лист
(6 мес бездействия физически не наступят раньше 6 мес после запуска; job писать тогда.)

### 8.12 Timezone
- `users.timezone`, автоопределение `Intl.DateTimeFormat().resolvedOptions().timeZone` на онбординге + перепроверка при каждом запуске; при расхождении диалог «Обновить?» (не чаще раза в 3 дня).
- **«Отменить» = живём по старой TZ** (ничего не сдвигается). Ночная защита: пока детектированная TZ ≠ сохранённой, уведомление подавляется, если сейчас ночь хотя бы в одной из двух зон.
- H-soft: смена TZ не ломает текущий логический день.

### 8.13 Платёжные каналы (переписан: test_dc и промокоды мертвы)
Единственный канал — **Telegram Stars** (работает с 7.4/7.5): тарифы Responsible, drop packs, бусты, Stars-предметы полки. Купоны = скидки на тарифы (7.5.1). Крипта — фаза 9+.

### 8.14 Безопасность и GDPR — в Ф8 только:
- Auto-delete тренировочных клипов через 7 дней (cron; Codex-аудит п.7).
- Ретеншн видео-обещаний (8.8a) + дисклеймер о хранении видео в онбординге + дисклоз «пол/цель/подготовка видны наставнику».
- Account deletion, data export, soft-delete 30 дней — **предзапускной чек-лист**.

### 8.15 Экономика
- Доход прилежного игрока: main-only ≈ 540 💧/мес, с light ≈ 990 💧/мес.
- Все цены — плейсхолдеры в админке; сведение в интерактивной модели **`ECONOMICS.html`** (в корне проекта): юнит-экономика Stars→$, COGS Gemini Vision/image + хостинг, сценарии 10/100/1000 юзеров. Требование юзера: «заработок пусть маленький, но нужен».
- **Gemini calibration v2 + пре-замер рамкой** (framing calibration) — НЕ входит в 8a–8e. **Условие старта (решение юзера, S50): появление качественных профессиональных демо-видео хотя бы одной группы упражнений** — калибровать оценки против плохих эталонов бессмысленно; делать вместе с reference-based оценкой (Gemini сравнивает клип игрока с эталонным демо — см. секцию «Демо-видео» выше).
- **Контент демо-видео — отдельный БОЛЬШОЙ трек юзера (не кодовый):** юзер над ним ещё не думал; «самая сложная часть»: нужны мнения профессиональных спортсменов + медицинские комментарии. Naming convention для файлов готова (секция «Демо-видео» выше). Планировать как самостоятельный проект параллельно 8b–8e.

### 8.16 Подфазы реализации (утверждены юзером, S50)
- **8a. Фундамент:** timezone + выбор 3 main-дней (онбординг + довыбор существующим) + стрик по расписанию + заморозки Ж3/М1 авто + уведомления утро/вечер + тихое окно. Довесок: хвосты S49 + живой smoke Job A/B.
- **8b. Light-режим:** контент 4 упражнений, light-сессия (30 💧), unlock/lock, стрик «любой день», активация со след. пн, дисклеймер-тесты.
- **8c. Магазин игрока:** витрина 4 карточек, докупка заморозок, фото-карточка (2 варианта/реролл/«без обработки»/мультяшки-дефолт), restore и смена графика контекстно.
- ✅ **8d. Полка наставника (реализована, Session 56, 2026-07-29):** видео-обещания полным циклом (приватный бакет `promises` + подписанные ссылки паре, ретеншн «галочка + 30 дней», напоминания R раз в 3 дня), Stars-предметы (каталог v1 freeze/photo_reroll, покупка = сразу выставление), слоты 2/4/6 от живой подписки, `gift_balance` + drop packs + дарение капель (RPC), выпил бустов X2 и легаси `shop_items`. Миграция 038. Живой смоук — чек-лист в SESSION_STATUS S56.
- **8e. Полировка:** гендерные тексты-словари, auto-delete клипов, админ-лимиты полки, TZ-диалог с ночным правилом.

### Отброшено при ревизии S50 (не делать)
Множитель ×3 отмены light; формула restore 10×days²; snooze-лимиты в ядре; двухколоночный баланс игрока; тест при покупке >100 💧; drops decay в Ф8; заморозки «только девушкам» бесплатные У ВСЕХ отклонено В ПОЛЬЗУ Ж3/М1; отправка видео-обещаний в чаты; хранение видео на устройстве как основное; покупка капель игроком за Stars.

---

### UX-полировка (остаток после Session 47)
> Всё остальное из этой секции (инвайт без кода, styled-аватар, купоны UX/удаление/once_per_user, live-пересчёт купона, поиск юзера, /start upsert, тир для особых аккаунтов + список, «Добавить игрока» в Action) — **переехало в постановку 7.5.1** (Session 47) и из BACKLOG удалено.

- **AdminCube → Платежи: общая перекомпоновка вкладки** (юзер, 2026-07-20): карточки платежей сделаны, верх ужат, но прокрутка/распределение высоты всё ещё неудобны в компактном окне Mini App. Полный редизайн вкладки — вместе с работой над магазином/платежами (после 7.5.1).
