# Antigravity Dashboard (Meta-Manager 🧠)

> Этот документ предназначен **только для Antigravity**. Claude Code (`.claudeignore`) сюда не смотрит. 
> Задача файла: хранить сжатую выжимку происходящего в проекте `Workout-Bot-FSM` и список задач по "уборке" (Clean Code/Clean Repo).

## 📊 Статус Проекта (по состоянию на Сессию 23/24)
- **Актуальный вектор:** Переход к Сессии 24.
- **Главный архитектурный сдвиг:** Subscription/Renewal/StreakFreeze (v4). Подписка теперь живет ТОЛЬКО на `partnerships.expires_at`. Убраны `upgrade` коды. Добавлены счетчики заморозки стриков (Shop vs Gift) и ручные дни отдыха для женщин.
- **Workflow в Claude Code:** Claude Code используется как слепой исполнитель (через CLI). Ему скармливаются готовые промпты шаг за шагом, чтобы экономить токены и минимизировать самодеятельность. Миграции Supabase выполняются агентом или вручную.
- **Текущая локация изменений:** БД (Миграция 020), `admin.py`, `promo.py`, `shop.py`. Фронтенд: `AdminCube`, `ActionCube`, `Notification Center` (внутри BondCube).

## 🧹 Санитарный Контроль (Clean Repo)
1. **Корневая папка замусорена черновиками:**
   - Временные HTML (`code dlya workout.html`, `promocode generate.html`, `INTRODUCTION.html`).
   - Логи и артефакты (`tsc_errors.txt`, `code_audit_report.pdf`).
   - **Решение:** Удалить или перенести в папку `research/archive/`.
2. **Перегрузка документами:**
   - В корне лежат `SESSION_23_PLAN.md`, `PROMPT_MINIMAL_TRANSITIONS.md`, `TEST_PLAN.md`.
   - **Правило уборки:** Как только все этапы Сессии 23 будут выполнены, сначала нужно **провести тесты по всем изменениям**, и только после успешного тестирования файл `SESSION_23_PLAN.md` должен быть удален. Остальные планы лучше спрятать в папку `_hidden_docs` или `sessions/`, чтобы в корне оставались только `CLAUDE.md`, `ROADMAP.md` и `SESSION_STATUS.md`.

## 📌 Моя Роль (Antigravity Role)
- **Технический надзор:** Я не пишу рутинный код для проекта (это делает Claude Code), но я проверяю архитектуру, слежу за папками, ищу неиспользуемые модули, пишу промпты и ревьюерские резолюции.
- **Держатель контекста:** Перед началом работы в Claude Code разработчик может закидывать мне идеи для согласования.

## 📝 Лог действий (Antigravity Changelog)
- **2026-04-20 (Cleanup):** Создана папка `_hidden_docs`. Настроено игнорирование этой папки и файла `ANTIGRAVITY.md` через `.claudeignore`, чтобы не тратить контекст Claude Code. В скрытую папку перенесены временные HTML-черновики, логи ошибок, аудит-репорт и устаревшие планы (`PROMPT_MINIMAL_TRANSITIONS.md`, `TEST_PLAN.md`).
