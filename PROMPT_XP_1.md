# PROMPT_XP_1 — экономика XP: начисление, уровни, награды, свободный день без камеры

> Постановка S67 (2026-09-02, Cowork) по итогам дизайн-интервью S66 (`ОТЧЁТ_S66_ДЛЯ_COWORK.md`)
> и уточнений юзера 01–02.09 (BACKLOG, секции 2026-09-01 и 2026-09-02). Патч задевает
> экономику и схему БД — решения юзера собраны здесь, изобретать нечего; сомнение о
> продуктовом смысле → СТОП и вопрос (шаг C PLAYBOOK).

**Meta:**
- ⚙️ Reasoning effort: xhigh
- 💭 Ultrathink: да (слово стоит в конце промпта)
- 👁 Transcript: Ctrl+O → `Verbose`

---

## Промпт для CC

Контекст читай так: `ОТЧЁТ_S66_ДЛЯ_COWORK.md` (части 2, 4, 5), BACKLOG секции
2026-09-01 и 2026-09-02. Перед кодом — сверка ключевых фактов по живому коду/БД
(урок №15): `workout_sessions.total_score` пишется; антифарм-гейт
`already_earned_today`; поля заморозок (`free_freezes_left` / `paid_freezes`);
`player_stats.global_score` мёртв. `pg_dump` перед патчем обязателен (libpq стоит,
рабочий хост пулера записан в SESSION_STATUS).

### Жёсткие рамки (задел → СТОП и вопрос)

1. **Антифарм (инвариант №3):** XP начисляется ТОЛЬКО там же и тогда же, где капли —
   максимум 1 main + 1 light в день. Свободные сессии не дают ничего и не могут
   стать обходным путём.
2. **Приватность (№1):** наставник видит уровень и XP-прогресс (§8.7 это разрешает),
   баланс капель игрока — по-прежнему никогда.
3. **Деньги (№2):** Stars/платежи не задеваются вообще; XP не покупается ни за что.
4. Изменение тренировочного флоу (ветка без камеры) → обновить чертёж
   `fsm_blueprints/200_workoutSessionMachine` 1:1 с кодом.
5. Миграции — идемпотентные (style-guide, Codex п.10).
6. Все константы экономики — в админ-настройки, не в хардкод.

### Кусок 1 — начисление XP + уровни (миграция)

1. `finish_session`: в существующий upsert добавить `global_score` —
   `xp = round(total_score × xp_mult)`, множители по типу сессии. Начисление строго
   под тем же антифарм-гейтом, что капли (повторные/внеплановые сессии XP не дают).
2. Настройки админки (7 чисел, дефолты):
   `xp_mult_main = 0.1`, `xp_mult_light = 0.1`, `level_base = 500`,
   `level_early_step = 1.25`, `level_late_step = 2.0`, `level_boundary = 3`.
   Стоимость уровня N: `N ≤ boundary: base × early_step^(N−1)`;
   `N > boundary: cost(boundary) × late_step^(N−boundary)`.
   Лестница-санити: 500 / 625 / 781 / 1562 / 3125 / 6250 / 12500…
   Кумулятив: L3 ≈ 1906 (≈ месяц при 3 main/нед), L6 ≈ 12.8k (≈ 7 мес).
3. Уровень и остаток шкалы вычисляются из общего XP формулой; экран показывает
   «внутри уровня: X из cost(N)» — шкала визуально обнуляется, счётчик один.
4. `/stats/me` (и досье наставника) отдают: xp, level, xp_in_level, level_cost.
5. Админка: экран с этими числами + живой предпросмотр первых 10 уровней
   (стоимость + кумулятив) — защита от бессмысленных значений.

### Кусок 2 — награды за уровень (заморозки)

- При взятии уровней 1/2/3 — **разовая** выдача +1/+2/+3 заморозок (решение юзера
  02.09; это подарок за уровень, НЕ прибавка к месячному запасу).
- Дефолт-рекомендация Cowork: выдача идёт в `paid_freezes`, и кап 3 действует только
  на докупку — уровневая выдача может временно превысить кап. Видишь проблему —
  СТОП и вопрос, не решай молча.
- Идемпотентность: уровень награждается один раз (например, `last_rewarded_level`
  в player_stats — реши и зафиксируй в Допущениях).
- Уведомление игроку: «Уровень N! +K заморозок ❄️».
- Награды уровней 4+ НЕ делать (в BACKLOG уже записано «придумать позже»).

### Кусок 3 — свободный день: light без камеры и без Gemini

- День вне плана (light-режим не куплен): кнопка «Приступим» активна, запускается
  light-сессия **без камеры и без оценки**: таймер + демо + счётчик упражнений.
- Экран упражнения — **на всю ширину** (места превью камеры нет — решение юзера).
- Никаких новых строк и кнопок (решение юзера 02.09): правду говорит САМА кнопка
  запуска — в свободный день она называется **«Тренировка без оценки»** вместо
  «Приступим». Внутри сессии дополнительных плашек не добавлять (урок №21: экран
  не говорит дважды об одном).
- Не пишет клипы в Storage, не вызывает Gemini, не даёт капель/XP, не влияет на
  стрик и не закрывает день. Как хранить такие сессии (отдельный `session_type`
  или флаг) — реши сам, зафиксируй в Допущениях; главное — они не плановые и не
  попадают в антифарм-поля.
- Купленный light-режим ничего не меняет: light-дни плановые, с камерой и
  начислениями, как сейчас.
- FSM-ветка no-camera → обновить чертёж 200 (рамка №4).
- ⚠️ Повторные сессии ПЛАНОВОГО дня остаются КАК ЕСТЬ (с камерой и Gemini) — это
  осознанная граница скоупа. Занеси в BACKLOG вопрос: «повторные сессии тоже без
  камеры? — жгут Gemini при нулевых начислениях, решить юзером».

### Кусок 4 — экраны

1. **Итоговый экран тренировки:** сейчас `avg_score` подписан как «XP» — враньё.
   Стало: «Техника: NN» (средняя оценка) отдельной строкой + «XP: +MM» (реальное
   начисление). В свободной тренировке цифр нет вообще — поддерживающая фраза,
   как у повторных сессий.
2. **Action:** полоса прогресса до следующего уровня (уровень + заполнение).
   Полный редизайн Action сюда НЕ входит — только полоса; направление «стрик
   крупно, XP полоской» из Части 3 отчёта.
3. **Слом стрика: ПЛАШКА вместо всплывающего окна** (юзер 02.09 отменил окно из
   Части 3 отчёта). Пока стрик сломан и восстановление доступно, на Action висит
   постоянная плашка: «Стрик прервался (было N). Восстановить — X 💧» с кнопкой.
   Механику и цену restore НЕ менять (текущая: 20💧 × дни, мин 60, кап 400).
   При нехватке капель — та же плашка БЕЗ кнопки, текст «стоит X 💧 — сейчас не
   хватает» (урок №13b). Когда плашка гаснет (восстановлен / начат новый стрик) —
   по текущей механике restore, реши и зафиксируй в Допущениях. Паттерн — урок
   №13c: важное состояние живёт постоянной строкой, не транзиентом.
4. **Цветную рамку уровня НЕ делать** — палитра договаривается на редизайне
   (BACKLOG). Слово для стрика на экранах пока не менять (решается на редизайне).

### Кусок 5 — фиксация

- BACKLOG: в записи 2026-08-27 про боковые планки дописать: «ложные нули теперь
  режут и XP (XP = баллы Gemini) — вес калибровки вырос»; пометить реализованное
  этим патчем.
- SESSION_STATUS: сессия S67, галочки, «Допущения CC».
- Гейты: `typecheck`, `build`, `py_compile`, `ruff --select F`. Коммиты по кускам.

---

## Смоук-чеклист (§1-E: по одному пункту, ожидание из БД ДО действия, подтверждение
## SELECT-ом; перед датозависимым — `TZ=Asia/Tashkent date`; все подмены печатать,
## подбирать так, чтобы конечное значение совпало с BEFORE)

0. **BEFORE-снимок:** player_stats Cell (global_score, drops_balance, заморозки,
   стрик-поля), настройки админки. ⚠️ У Cell стоят тестовые **200 капель** со смоука
   S66 — в откате вернуть живые значения: **11 капель, 0 paid_freezes** (Часть 6
   отчёта).
1. **Миграция:** применена; повторный прогон не падает (идемпотентность).
2. **Плановая тренировка (игрок в Лондоне, полная):** XP = round(total_score × 0.1)
   начислен — сверка SELECT-ом с суммой баллов сессии; капли начислены как раньше;
   экран итога: «Техника NN» + «XP +MM»; полоса уровня на Action двинулась.
3. **Антифарм:** повторная сессия того же дня XP не даёт. Живой повтор или приём
   «устаревший экран»/SQL-подмена дневных полей с возвратом — как в S64.
4. **Взятие уровня:** XP подложкой доводится до порога → уровень вырос, разовые
   заморозки выданы (+1 на L1), повторная выдача НЕ происходит; уведомление пришло.
5. **Свободный день:** в день без плана кнопка активна; сессия идёт БЕЗ камеры,
   экран на всю ширину, строка «без оценки и наград» на месте; в конце — ноль
   начислений (SELECT: drops/global_score/стрик не двинулись), **клипов в бакете
   не прибавилось** (SELECT по storage.objects), вызовов Gemini нет (по логам).
6. **Плашка слома стрика:** при сломанном стрике видна постоянно (не пропадает при
   перезаходе); при нехватке капель — без кнопки, текст с ценой (подмена баланса
   под открытым экраном, с возвратом); после восстановления — гаснет.
7. **Наставник:** в досье игрока виден уровень/XP-прогресс; баланс капель игрока
   НЕ виден нигде (инвариант №1 — пройти глазами).
8. **Откат:** булева сверка с BEFORE 1:1; Cell = 11 капель / 0 paid_freezes /
   живой стрик; настройки админки — боевые дефолты.
9. **Гейты зелёные** на финальном коммите; чертёж 200 обновлён.

После смоука: ломавшееся → уроки PLAYBOOK; статус S67; следующее по плану —
редизайн Action (палитра, рамка уровня, слово для стрика) отдельной постановкой.

ultrathink
