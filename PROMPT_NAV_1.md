# PROMPT_NAV_1 — Ревизия навигации (постановка S64, 2026-08-13)

> Источник решений: `SESSION_STATUS.md` → блок «Session 64», решения **S64-1…S64-9**.
> Кода в сессии-постановке не писали; этот файл — задание для CC.

---

**Meta:**
- ⚙️ Reasoning effort: `high`
- 💭 Ultrathink: нет
- 👁 Transcript: Ctrl+O → `Verbose` (multi-file UI-рефакторинг, нужны диффы)

---

## Промпт для CC

Прочитай `CLAUDE.md`, затем блок **Session 64** в `SESSION_STATUS.md` (решения S64-1…S64-9 —
источник правды по этой задаче) и этот файл. Задача: **ревизия навигации игрока** — убрать
эксклюзивный контент из-под скрытых жестов. Экономику, цены, формулы, БД и бэкенд-логику
НЕ трогаем: миграций в этом патче быть не должно (единственное исключение — если найдёшь,
что перенос UI требует поля, которого нет; тогда СТОП и вопрос юзеру).

### Что сейчас в коде (сверено при постановке — перепроверь перед правкой)

- `frontend/src/App.tsx`: `HOLD_DASHBOARD = 3000` — удержание 3с в режиме `chaos` включает
  layout `dashboard`; `HOLD_FOR_SWIPE = 400` + `SWIPE_UP_THRESHOLD = 80` — «удержание +
  свайп вверх» переключает тему во ВСЕХ режимах; тема живёт в `useState` App и раздаётся
  через `ThemeContext` (только значение, без сеттера).
- `frontend/src/components/shared/DashboardPanel.tsx` — единственное место, где рендерится
  `PlayerSchedulePanel`. То есть неделя, заморозки, время напоминания и **платная смена
  графика (100💧)** доступны игроку ТОЛЬКО через скрытое удержание.
- `frontend/src/components/cubes/BondCube.tsx:166` — кнопка «Профиль и настройки»
  **мёртвая**: `onClick={(e) => e.stopPropagation()}`, никуда не ведёт. Явного входа
  в профиль в приложении нет вообще.
- `PlayerSchedulePanel` содержит 6 блоков: имя+звание · «Твоя неделя» (week-strip +
  подписи light/трайал) · заморозки · время утреннего напоминания · «Дни main-тренировок»
  с платной сменой (ConfirmSpendModal + строки pending/cooldown) · инфо-строка про light.

### Что сделать

**1. Жест удержания (S64-2).**
- Порог `HOLD_DASHBOARD`: 3000 → **5000 мс**. Константу назвать по смыслу и оставить
  одним числом в одном месте (юзер будет крутить её на смоуке; допустимый диапазон 4000–6000).
  Заодно поправь устаревший комментарий `App.tsx:105` («Удержание 2.5с» над 3000).
- **Видимый отклик удержания (S64-9г, урок №3 PLAYBOOK).** 5 секунд «в никуда» читаются
  как поломка. Нужна проекция состояния: пока палец держат — заметная индикация прогресса
  (кубы плавно замирают / круговой индикатор / затемнение — форму выбери сам, дёшево и без
  новых зависимостей), отмена при отпускании и при уходе пальца в свайп.
- Поведение не меняется: удержание в `chaos` открывает статический вид содержимого кубов
  (текущий `dashboard`). Это остаётся **зеркалом**: после переездов ниже на нём не должно
  быть НИ ОДНОЙ функции, недоступной иначе.
- Выход со сводки: крестик оставить + добавить закрытие тапом по фону (тап вне контента
  панели). ⚠️ **Механизм зафиксирован:** закрытие по фону вешать на `onClick` слоя-фона,
  НЕ на pointer-события. Причина: существующие защиты внутри — `onClick`+`stopPropagation`
  (`PlayerSchedulePanel.tsx:149`, `DashboardSection.tsx:123`), а оверлей слушает
  `onPointerDown/Up` (`App.tsx:279`) — на pointer-уровне эти защиты не срабатывают и
  тап внутри панели закроет экран.
- Отдельную видимую кнопку «открыть сводку» НЕ добавлять.

**2. Жест смены темы — удалить (S64-4).**
- Ветку «удержание + свайп вверх → `setTheme`» из `handleGestureUp` убрать целиком,
  вместе с осиротевшими константами, если они больше нигде не нужны.
- Вместо неё — **круглая кнопка с иконкой** на экране профиля (п.4). Не тумблер.
  Иконка показывает ЦЕЛЬ переключения: в тёмной теме ☀️, в светлой 🌙.
- Техническое: `ThemeContext` сейчас отдаёт только значение. Нужен путь к сеттеру.
  Реши сам (расширить контекст объектом `{theme, toggleTheme}` с правкой потребителей,
  либо отдельный `useThemeToggle`), но: `useTheme()` в `RoleTransition.tsx` не должен
  сломаться (это ЕДИНСТВЕННЫЙ потребитель — проверено), и решение зафиксируй блоком
  «Допущения» в `SESSION_STATUS.md`.
- **Тема сохраняется на устройстве (S64-9а).** Сейчас `App.tsx:47` — обычный `useState`,
  выбор темы теряется при перезаходе. Пока переключение было скрытым жестом, это никто
  не замечал; у явной кнопки сброс прочитается как баг. Сохранять в `localStorage`
  (не в БД: устройство — правильная область для оформления), при старте — читать,
  при отсутствии значения дефолт `dark` как сейчас.

**3. Расписание → Action-куб (S64-3, S64-7).**
Переносим в Action (вид игрока) блоки: **«Твоя неделя»** (week-strip + подписи light и
трайала) · **заморозки** · **«Дни main-тренировок» с платной сменой** · **инфо-строка light**.
- Форму подачи внутри Action выбери сам (секция в куб или подэкран в стеке) — критерий:
  кнопки запуска тренировки остаются первым, что видит игрок; расписание не выталкивает
  их за пределы первого экрана.
- **Разделение компонента (уточнение по S64-7).** `PlayerSchedulePanel` придётся разрезать:
  имя+звание и время напоминания уходят в профиль, остальное — в Action. Раздели на части
  так, чтобы **копипасты не было**, и обе точки рендера собирались из одних и тех же кусков.
- **Что остаётся на сводке (S64-5а).** Сводка сохраняет ПОЛНЫЙ прежний состав как зеркало:
  и блоки, уехавшие в Action, и блоки, уехавшие в профиль. Меняется только их статус —
  нигде на сводке больше нет эксклюзива. Функциональность блоков на сводке не режем.
- Гейты смоука S63 обязаны сохраниться дословно (уроки 13a/13b/13c PLAYBOOK):
  окно `ConfirmSpendModal` перед списанием · кнопка смены на кулдауне серая-но-живая
  с причиной ДО окна траты · постоянная строка «📅 С понедельника …» про отложенное
  вступление (не тост) · границы формы считаются из живых данных, не из снапшота.

**4. Профиль → настоящий экран (S64-4, S64-6, S64-7, S64-8).**
- Кнопка «Профиль и настройки» в `BondCube` должна открывать экран. Содержимое **у игрока**:
  **имя + звание** (🏅, как сейчас в `player-identity`) · **время утреннего напоминания**
  (input + сохранение, поведение не менять) · **круглая кнопка темы ☀️/🌙**.
- **Профиль обязан быть и в виде наставника (S64-8).** Сейчас кнопка живёт внутри
  `PlayerBond` и видна только при `canPlay` (`BondCube.tsx:98`) — у наставника без роли
  игрока (боевой Oil) после удаления жеста не осталось бы способа сменить тему. В виде R
  профиль содержит только **имя + кнопку темы** (расписания и напоминаний у наставника нет).
- Экран в стиле остальных подэкранов куба, с явным закрытием.
- ⚠️ Если экран рендерится через `createPortal` — красить ТОЛЬКО `--tg-theme-*`
  переменными (урок №2 PLAYBOOK: тема-классы в порталы не достают).
- Звание в шапке Action-куба (S62-4, хотфикс 6) **оставить** — оно там не ради профиля.

**5. Микро-райдер: склонение «игрок(ов)» (хвост шлифовки S63).**
Два места, оба починить, переиспользуя существующий помощник (`frontend/src/utils/tierText.ts`,
паттерн `pluralDays` в `DashboardSection.tsx:33`) — третий велосипед не заводить:
- `backend/services/tier_pricing.py:87` (`invoice_description`) — «(до {limit} игрок(ов))».
  Это и есть текст счёта продления из шлифовки S63. **Разрешённое исключение** из правила
  «питон не трогаем» — только эта строка.
- `frontend/src/components/shared/RenewalScreen.tsx:199` — «до {limit} игроков», при limit=1
  даёт «до 1 игроков». Строка `:261` («допускает N игрок(ов)») — тот же класс, тоже починить.

**6. Иконка капель в строках-сводках (S64-9б).**
`DashboardSection.tsx:61` рисует баланс капель игрока иконкой ⭐ — путаница двух валют
(⭐ = реальные деньги наставника, 💧 = труд игрока; см. CLAUDE.md «Project Context»).
Заменить на 💧. Строку наставника (`:71`, там ⭐ законно) не трогать.

**7. Строки сводки «👤 Профиль» / «⚙️ Настройки» (S64-9в).**
`DashboardSection.tsx:83,112` обещают вход в профиль, но `App.tsx:285` выбрасывает
аргумент `sub` — строка просто открывает куб. Дособрать проброс `sub`, чтобы эти строки
открывали новый экран профиля. Иначе сводка рекламирует вход, которого нет, и требование
«сводка = зеркало» не выполняется.

### Границы

- Ролевой переключатель и строки-сводки кубов на дашборде — не трогать (S64-5б),
  КРОМЕ точечных исключений п.6 и п.7 выше: роль переключается и внутри кубов,
  эксклюзива на сводке нет.
- Вид наставника (R) меняем ТОЛЬКО в объёме п.4 (профиль с именем и кнопкой темы) —
  полки, витрины, досье игрока не касаемся.
- Инварианты `CLAUDE.md` не задеваются (двигаем UI). Если по ходу увидишь, что задеваются —
  СТОП и вопрос юзеру.
- Verify перед коммитом: `npm run typecheck` (`tsc -b --force`), `npm run build`,
  `python -m py_compile backend/services/tier_pricing.py`, `ruff --select F`.
- По завершении обнови `SESSION_STATUS.md` (блок S64: что сделано + «Допущения»)
  и `frontend/public/orientation-lis81hed2ymoso80.html` (состояние + строка в «📜 Хроника»).

---

## Смоук-чеклист (PLAYBOOK §1-E; прогон юзером после реализации)

Формат: по ОДНОМУ пункту · ожидание формулируется ДО действия · где есть БД —
подтверждение SELECT-ом через `my-supabase` · устройства: **Cell — iPhone (игрок)**,
**Oil — десктоп (наставник)**. Ожидание по датам: **с пн 17.08 у Cell реально идёт
неделя light** (следы смоука S63) — week-strip покажет light-дни, это не баг.

**0. BEFORE.** Снять SELECT-ом (точные таблицы сверь по коду: `users` /
`player_stats`): `main_days`, `pending_main_days`, `morning_reminder_time`,
`paid_freezes`, `free_freezes_left`, `next_change_available_at`, `drops_balance`
игрока. Записать.

**1. Порог удержания + отклик.** Держать палец 3 секунды → экран НЕ меняется (было:
менялся). Держать 5+ секунд → открывается статический вид кубов. По ходу удержания
на экране виден прогресс жеста; отпустил раньше — индикация исчезла, экран не открылся.

**2. Выход со сводки.** Крестик закрывает. Тап по фону закрывает. Тап по элементу
внутри панели (например, по полю недели) — НЕ закрывает.

**3. Жест темы мёртв.** Удержание + свайп вверх на каждом из трёх экранов (кубы,
fullscreen-куб, сводка) → тема НЕ меняется ни в одном.

**4. Кнопка темы в профиле.** Bond → «Профиль и настройки» открывается (было: кнопка
не работала). В тёмной теме на кнопке ☀️ → тап → приложение светлое, иконка стала 🌙 →
тап → вернулось. Пройти оба направления. Затем: выбрать светлую, **полностью закрыть
и открыть мини-апп** → тема осталась светлой (S64-9а).

**4б. Профиль у наставника (S64-8).** На десктопе Oil (аккаунт без роли игрока):
Bond → профиль открывается, в нём имя и кнопка темы, расписания и напоминаний НЕТ.
Тема переключается в обе стороны.

**5. Профиль: время напоминания.** Изменить время, сохранить → SELECT
`users.morning_reminder_time` показывает новое значение. Закрыть/открыть профиль —
значение на месте (не снапшот). После проверки вернуть исходное время (п.0).

**6. Профиль: имя и звание.** Имя игрока и 🏅 звание видны. Звание при этом
по-прежнему видно и в шапке Action (S62-4 не потерян).

**7. Расписание в Action.** Открыть Action → неделя, заморозки, дни main, инфо-строка
light видны БЕЗ всяких жестов. Кнопка запуска тренировки видна на первом экране,
не уехала под расписание.

**8. Смена графика — путь кулдауна (урок 13b).** Ожидание из БД: если
`next_change_available_at` в будущем — кнопка смены серая-но-живая, причина («следующая
смена доступна с …») видна ДО окна траты; окно подтверждения НЕ открывается; капли целы
(SELECT `drops_balance` до/после = равны).

**9. Смена графика — окно и путь «Отменить» (урок 13a).** ⚠️ **Покупку НЕ совершать.**
Списание и запись pending доказаны смоуком S63 — этот патч двигает UI, RPC не трогает;
а реальная покупка перезаписала бы **живую оплаченную смену Cell (вт·чт·сб с 17.08)** и
потратила бы её капли. Проверяем только то, что мог сломать переезд: тестовой подменой
`next_change_available_at` в прошедшую дату открыть форму → выбрать 3 дня → окно
`ConfirmSpendModal` показывает цену и остаток ИЗ ЖИВЫХ данных (не снапшот, 13a) →
«Отменить» → SELECT: `drops_balance` и `pending_main_days` не изменились → вернуть
`next_change_available_at` как было (сверка с п.0).

**9б. Строка отложенного вступления (урок 13c).** Если прогон идёт ДО пн 17.08:
в Action у Cell обязана быть видна постоянная строка «📅 С понедельника 17.08: вт·чт·сб»
(живой pending от реальной покупки S63); закрыть/открыть экран — строка на месте.
Если прогон ПОСЛЕ 17.08 — pending уже применён, строки нет, и это правильно: пункт
пропускается с пометкой (код строки переносится компонентом без изменений).

**10. Заморозки.** Цифра совпадает с `free_freezes_left + paid_freezes` из БД,
подпись не врёт про происхождение («в запасе», не «купленных» — S63).

**11. Сводка = зеркало.** На статическом виде нет ни одной функции, которой нет в кубах:
пройти по всем его блокам и для каждого назвать явный дом. Строки «👤 Профиль» и
«⚙️ Настройки» ведут в новый экран профиля, а не просто открывают куб (S64-9в).

**12. Инварианты + иконки валют.** В режиме наставника нигде не появился баланс капель
игрока; у игрока нигде нет Stars/цен в ⭐ (§1 инвариантов). Отдельно: в строке-сводке
игрока баланс идёт с 💧, а не ⭐ (S64-9б); у наставника ⭐ на месте, где это Stars.

**13. Откат.** Вернуть подменённые поля к значениям из п.0, сверить булевым сравнением
в SQL (1:1, как в S63).

**14. Verify-гейты** на финальном коммите: `npm run typecheck`, `npm run build` — зелёные.
Grep по репо: `игрок(ов)` и `игроков` в затронутых местах больше не дают «1 игроков» /
«1 игрок(ов)» — проверить оба файла (питон + `RenewalScreen`) чтением строк, а не только
грепом бандла (в бандле питоновской строки нет по определению — на неё грепом не проверишь).
