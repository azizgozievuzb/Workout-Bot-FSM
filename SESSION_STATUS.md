# SESSION STATUS — S67 закрыта 2026-09-05. Следующее: кнопка после закрытого дня

> **Следующая работа (постановка готова):** повтор в УЖЕ закрытый день должен идти как
> свободная тренировка — кнопка «Тренировка без оценки», короткий формат, условие
> «день закрыт». Решение юзера 05.09, полный текст — `BACKLOG.md`, секция
> «2026-09-02 (вечер)», п.1 (помечен ✅ РЕШЕНО). Код ещё НЕ тронут.
>
> Дальше — редизайн Action отдельной постановкой (палитра, цветная рамка уровня, слово
> для стрика, состав статус-строки).
>
> ⏳ **Веха S65 (жёсткий срок):** даунгрейд Supabase на Free 15–18.09, дедлайн **23.09** —
> тогда списываются следующие $25.
>
> 🔍 **Висит непрочитанным:** `REVIEW_FABLE51.md` — независимая ревизия проекта
> (Fable 5.1, только чтение), заказана 02.09. К смоуку отношения не имеет, находки не
> разобраны.

История S67 целиком (код, миграция, все 10 пунктов смоука, находки) —
`_archive/SESSION_LOG.md`, верхний блок.

---

## Что сделано в S67

**Код (4 коммита):** XP-начисление и лестница уровней, награды заморозками за уровни
1–3, вкладка «XP» в админке (семь настроек в БД, не в хардкоде), свободный день (light
без камеры, Gemini и начислений), экраны — «Техника»/«XP» на итоге, полоса уровня на
Action, плашка слома стрика. Миграция **039** применена, дамп снят
(`~/backups/workout_bot_2026-09-02.dump`, вне репо).

**Смоук пройден целиком, 10 пунктов из 10** (02–05.09, живьём с телефона). Ключевое:

* **Начисления сошлись на двух настоящих тренировках** (03.09 и 05.09), разные суммы
  баллов — XP = `round(сумма × 0.1)` совпал оба раза, капли и стрик как раньше.
* **Антифарм работает:** повтор в тот же день — 0 капель, 0 XP, экран без цифр.
* **Уровень выдаётся один раз:** заморозка +1, уведомление, повторно не выдаётся
  (идемпотентность через `last_rewarded_level`).
* **Свободный день чист:** ни одного вызова Gemini, ни одного клипа в Storage, строка
  `player_stats` не тронута вообще (проверено по `updated_at`).
* **Инвариант №1 не задет:** наставник видит уровень и прогресс, баланса капель игрока
  нет нигде.

**Три хотфикса по ходу смоука:** `a4e6de6` кэш XP-настроек, `7725bf9` подпись «XP» на
полосе уровня, `b9e2fa8` уведомления показывают русский текст из БД (чинит и старый
дефект `freeze_consumed`, живший до S67).

**Уроки в PLAYBOOK:** №25 (один текст в двух местах разъезжается), №26 (оборванная
сессия оставляет `.git/index.lock`).

---

## Состояние аккаунта Cell после смоука (подмен НЕТ)

Все подмены сняты 05.09 20:3x. Откат сделан **не** «к BEFORE 1:1», а по решению юзера:
снято только подстроенное смоуком, заработанное руками оставлено.

| поле | значение | откуда |
|---|---|---|
| `global_score` | **85** | 43 (чт 03.09) + 42 (сб 05.09), обе тренировки настоящие |
| `drops_balance` | **183** | заработанное + возврат 60 за тестовое восстановление |
| `paid_freezes` / `free_freezes_left` | 1 / 0 | как до смоука |
| `current_streak` / `best_streak` | 3 / 7 | стрик заработан живьём, не трогали |
| `last_workout_date` / `last_closed_day` / `last_main_drops_day` | 2026-09-05 | настоящий день |
| `last_rewarded_level` | 0 | наград по-настоящему ещё не было |
| `lost_streak_len` / `lost_streak_at` | null / null | слом не взводили (решение юзера) |
| `app_settings` | боевые дефолты | 0.1 / 0.1 / 500 / 1.25 / 2.0 / 3 / `{"1":1,"2":2,"3":3}` |

---

## Хвосты (не блокеры)

1. **«🎉 Уровень N!» на экране итога живьём не видели.** Уведомление проверено, БД
   проверена, но сама строка на экране итога показывается только в неповторной сессии —
   а уровень мы брали подменой XP на повторе. Проверится само, когда игрок возьмёт
   уровень по-настоящему (~500 XP, при нынешнем темпе ≈ 3–4 недели), либо специально
   подменой перед плановой тренировкой.
2. **Пароль БД засветился в переписке 02.09.** Утечки за пределы машины нет; сбросить в
   Dashboard — дешёвая перестраховка. API-ключи бэкенда это не рвёт.
3. **Находки смоука, ушедшие в BACKLOG, не чинились:** нестабильность оценщика Gemini
   (1–3 ложных нуля за сессию), демо-видео играет во время «Приготовьтесь», чип заморозок
   на Action читает мёртвое поле, четыре находки по часовым поясам.
